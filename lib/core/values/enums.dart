enum RequestType {
  get,
  getWithToken,
  post,
  postWithToken,
  postWithHeaders,
  postWithOnlyHeaders,
  delete,
  putWithHeaders
}

// ignore: constant_identifier_names
enum Status { INITIAL, LOADING, COMPLETED, ERROR }
